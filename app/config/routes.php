<?php

    /**
     * $config['routes']        Routes configuration
     */
    $config['routes'] = array(
        'useDefault'        =>  true,           // Use the default Controller and Action if the specified route doesn't exist
        'defaultController' =>  'home',         // Default controller
        'defaultAction'     =>  'index'         // Default action
    );

?>
